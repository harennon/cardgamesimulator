{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --cached --name-only | grep -q '^CHANGELOG.md$' || echo 'BLOCKED: CHANGELOG.md is not staged. Add a changelog entry before committing.'"
          }
        ]
      }
    ]
  }
}
