{
  "$comment": "LLD 77 §5.5 / 77a — captured/fixture diff modeling the REAL prod baseline (reconciled to the 2026-07-04 capture in scripts/fixtures/captures/). It is the autonomous-safe stand-in for the linked gate, so it mirrors live prod: (1) the lone residual is function:public:increment_player_stats, which `supabase db diff --linked` re-emits as a no-op CREATE OR REPLACE — acknowledged in expected-diff.allowlist.json (issue #91) so the gate subtracts it and exits 0; (2) `pending` is ONLY 010_create_game_history.sql — 009_add_game_config.sql is APPLIED to prod (migration list Remote=009), so it is NOT pending. `pending` must mirror the allowlist's expectedPending or the gate fails 'Stale expectedPending'.",
  "objects": [{ "object": "function:public:increment_player_stats" }],
  "expectedFromPending": [],
  "pending": ["010_create_game_history.sql"]
}
