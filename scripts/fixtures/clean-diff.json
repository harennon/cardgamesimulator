{
  "$comment": "Base is Phase 2 post-011 (010 + 011 applied to prod). 012_prune_game_history.sql (LLD 149) is pending (not yet applied), so it is listed in pending here AND in expected-diff.allowlist.json's expectedPending (fixture coupling). 012's prune_game_history function self-attributes to pending 012 and drops as benign, so it never appears as residual — objects still mirrors only increment_player_stats (#91), the cosmetic re-emission that stays acknowledged. After 012 is applied to prod, remove it from pending here + expectedPending.",
  "objects": [{ "object": "function:public:increment_player_stats" }],
  "expectedFromPending": [],
  "pending": ["012_prune_game_history.sql"]
}
