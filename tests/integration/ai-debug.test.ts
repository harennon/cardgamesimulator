/**
 * Minimal smoke-test used during LLD 122 development to debug AI game startup.
 * Kept as a passing test so the file doesn't break the suite.
 */
import { describe, it } from "vitest";

describe("AI game debug smoke", () => {
  it("placeholder — debug test no longer needed", () => {
    // No-op: the real tests are in ai-completion.test.ts
  });
});
