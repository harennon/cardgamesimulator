{
  "$comment": "LLD 77 §5.3 — drift-gate expected-diff allowlist. expectedPending lists the in-tree migration filenames NOT YET applied to prod on this branch (the diff hunks they produce are benign and subtracted by the gate). acknowledgedResidual lists reviewed, issue-linked residual diffs that are not yet cleaned up; it MUST stay empty after #83 — a non-empty entry is a deliberate, auditable exception, never a default. Migration 009 (add games.deck_rounds_target, LLD 93) is in-tree but not yet applied to prod, so it sits in expectedPending until the gated prod db push (then it is removed in cleanup). acknowledgedResidual stays empty.",
  "expectedPending": ["009_add_games_deck_rounds_target.sql"],
  "acknowledgedResidual": []
}
