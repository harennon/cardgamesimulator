{
  "$comment": "LLD 77 §5.3 — drift-gate expected-diff allowlist. expectedPending lists the in-tree migration filenames NOT YET applied to prod on this branch (the diff hunks they produce are benign and subtracted by the gate). acknowledgedResidual lists reviewed, issue-linked residual diffs that are not yet cleaned up; it MUST stay empty after #83 — a non-empty entry is a deliberate, auditable exception, never a default. Migrations 001-006 are applied to prod, so both lists are empty on this branch.",
  "expectedPending": [],
  "acknowledgedResidual": []
}
